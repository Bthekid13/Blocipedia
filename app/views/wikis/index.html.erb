<h1>Wikis</h1>
<% @wikis.each do |wiki| %>
<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to wiki.title, wiki %>
      <%= link_to 'Index', wikis_path %>
    </h4>
  </div>
</div>
<% end %>
